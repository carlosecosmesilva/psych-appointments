-- Database initialization script for PsychAppointments
-- This script will run automatically when the PostgreSQL container starts for the first time
-- Create extensions if needed
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- You can add initial data here if needed
-- Example:
-- INSERT INTO users (id, name, email) VALUES (uuid_generate_v4(), 'Admin', 'admin@example.com');
-- Note: Entity Framework migrations will handle table creation
-- This script is for any initial setup or seed data